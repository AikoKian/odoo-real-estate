<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_users_view_form" model="ir.ui.view">
        <field name="name">res.users.view.form.inherit.estate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            
            <!-- 1. Agregamos la pestaña en el notebook (Lo de ayer) -->
            <xpath expr="//notebook" position="inside">
                <page string="Real Estate">
                    <field name="property_ids"/>
                </page>
            </xpath>

            <!-- 2. Agregamos el botón en la caja superior (Lo de hoy) -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="%(estate_property_action)d" type="action" class="oe_stat_button" icon="fa-home">
                    <div class="o_stat_info">
                        <field name="property_ids" widget="statinfo" string="Properties"/>
                    </div>
                </button>
            </xpath>

        </field>
    </record>
</odoo>