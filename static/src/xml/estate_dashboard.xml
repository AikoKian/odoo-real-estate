<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="estate.Dashboard" owl="1">
        <div class="o_estate_dashboard p-4">
            <h1 class="mb-4">Métricas de Inmuebles en Tiempo Real</h1>
            
            <div class="row mt-4">
                <!-- Tarjeta de Propiedades Vendidas -->
                <div class="col-md-4">
                    <div class="card text-white bg-success mb-3 shadow-sm">
                        <div class="card-header">
                            <i class="fa fa-money me-2"></i> Propiedades Vendidas
                        </div>
                        <div class="card-body">
                            <h1 class="card-title display-4 text-center">
                                <t t-esc="state.soldProperties"/>
                            </h1>
                        </div>
                    </div>
                </div>

                <!-- Tarjeta de Propiedades Nuevas -->
                <div class="col-md-4">
                    <div class="card text-white bg-primary mb-3 shadow-sm">
                        <div class="card-header">
                            <i class="fa fa-building me-2"></i> Nuevos Ingresos
                        </div>
                        <div class="card-body">
                            <h1 class="card-title display-4 text-center">
                                <t t-esc="state.newProperties"/>
                            </h1>
                        </div>
                    </div>
                </div>

                <!-- Tarjeta de Propiedades Canceladas -->
                <div class="col-md-4">
                    <div class="card text-white bg-dark mb-3 shadow-sm">
                        <div class="card-header">
                            <i class="fa fa-ban me-2"></i> Propiedades Canceladas
                        </div>
                        <div class="card-body">
                            <h1 class="card-title display-4 text-center">
                                <t t-esc="state.canceledProperties"/>
                            </h1>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Fila para el Gráfico -->
            <div class="row mt-5">
                <div class="col-md-6 offset-md-3">
                    <div class="card shadow-sm">
                        <div class="card-header bg-white">
                            <h4 class="mb-0 text-dark"><i class="fa fa-pie-chart me-2"></i> Distribución de Propiedades</h4>
                        </div>
                        <div class="card-body">
                            <!-- t-ref permite que JS encuentre este lienzo exacto -->
                            <canvas t-ref="chartCanvas"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Botón conectado a la nueva función de refresco -->
            <div class="mt-3">
                <button class="btn btn-secondary shadow-sm" t-on-click="refreshDashboard">
                    <i class="fa fa-refresh me-1"></i> Actualizar Datos
                </button>
            </div>
        </div>
    </t>
</templates>